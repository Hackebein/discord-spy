{
  "name": "discord-spy",
  "version": "1.0.0",
  "description": "A Discord userbot which logs a specified server's chat into another channel",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "discord.js": "^11.3.0"
  }
}
